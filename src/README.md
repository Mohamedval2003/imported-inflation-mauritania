# Source Modules

This directory is reserved for reusable Python modules if the notebook is modularised in a future version. The current public release preserves the original notebook methodology and calculations without refactoring them into separate modules.
